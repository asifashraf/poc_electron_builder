class FindOptions{
    deep;

    order;//name, size, type, extension, created, modified,
    orderAsc; //bool

    pattern;
    patternType;
    /*ext;
    prefix;
    openTextSearch;
    regex;
    fullMatch;*/

    skip;
    take;

}

export default FindOptions;